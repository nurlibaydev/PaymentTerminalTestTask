<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PaymentTerminalTestTask" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/color_primary</item>
        <item name="background">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.PaymentTerminalTestTask" parent="Base.Theme.PaymentTerminalTestTask" />


    <!-- Специальный стиль для TextInputLayout с outline и скруглением -->
    <style name="Widget.App.TextInput.Outline" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">1dp</item>
        <item name="boxCornerRadiusTopStart">8dp</item>
        <item name="boxCornerRadiusTopEnd">8dp</item>
        <item name="boxCornerRadiusBottomStart">8dp</item>
        <item name="boxCornerRadiusBottomEnd">8dp</item>
        <item name="hintTextAppearance">@style/Widget.App.TextInput.Hint</item>
        <item name="helperTextTextAppearance">@style/Widget.App.TextInput.Helper</item>
    </style>

    <style name="Widget.App.TextInput.Hint" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textColor">@color/input_hint</item>
    </style>

    <style name="Widget.App.TextInput.Helper" parent="TextAppearance.MaterialComponents.Caption">
        <item name="android:textColor">@color/input_hint</item>
    </style>

</resources>